const $=(id)=>document.getElementById(id);let page=1,total=0,limit=25,records=[];const fields=['search','dateFrom','dateTo','carrier','driver','equipment','direction','loadType'];function params(extra={}){const p=new URLSearchParams({page,limit,...extra});for(const f of fields){if($(f).value)p.set(f,$(f).value)}return p}async function load(){const [sum,res]=await Promise.all([fetch('/api/checkins/summary').then(r=>r.json()),fetch('/api/checkins?'+params()).then(r=>r.json())]);$('total').textContent=sum.total||0;$('today').textContent=sum.today||0;$('inbound').textContent=sum.inbound||0;$('outbound').textContent=sum.outbound||0;records=res.data||[];total=res.total||0;render()}function fmt(d){return d?new Date(d).toLocaleString():''}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function render(){$('count').textContent=`${total} records`;$('pageInfo').textContent=`Page ${page} of ${Math.max(1,Math.ceil(total/limit))}`;$('rows').innerHTML=records.length?records.map((r,i)=>`<tr><td>${fmt(r.created_at)}</td><td>${esc(r.et_number)}</td><td>${esc(r.driver_name)}</td><td>${esc(r.carrier_name)}</td><td>${esc(r.equipment_no)}</td><td>${esc(r.load_type_group||r.entry_task)}</td><td><span class="pill">${esc(r.direction)}</span></td><td>${esc(r.load_no||r.receipt_id||r.po_no||r.reference_no)}</td><td>${esc(r.customer)}</td><td>${esc(r.door_assignment)}</td><td><button onclick="detail(${i})">Review</button></td></tr>`).join(''):`<tr><td colspan="11" class="empty">No check-ins found.</td></tr>`}function detail(i){const r=records[i];const labels={created_at:'Date',et_number:'ET #',driver_name:'Driver',driver_phone:'Phone',driver_license:'License',driver_email:'Email',carrier_name:'Carrier',usdot:'USDOT / MC',vehicle_type:'Vehicle Type',license_plate:'Plate',equipment_type:'Equipment Type',equipment_no:'Trailer / Container',entry_task:'Entry Task',load_type_group:'Load Type',direction:'Inbound / Outbound',reference_no:'Reference #',load_no:'PO / RN / Load #',receipt_id:'Receipt ID',po_no:'PO #',load_id:'Load ID',wms_load_no:'WMS Load #',customer:'Customer',customer_id:'Customer ID',customer_code:'Customer Code',door_assignment:'Door Assignment',comments:'Comments',photo_count:'Photo Count',identity_url:'Identity Link',basic_info_attached:'Basic Info Attached',trip_info_attached:'Trip Info Attached',email_notification_sent:'Email Notification Sent',status:'Status'};$('detailBody').innerHTML=Object.entries(labels).map(([k,l])=>`<div><b>${l}</b><span>${k==='created_at'?fmt(r[k]):esc(r[k])}</span></div>`).join('');$('detail').showModal()}$('apply').onclick=()=>{page=1;load()};$('clear').onclick=()=>{fields.forEach(f=>$(f).value='');page=1;load()};$('prev').onclick=()=>{if(page>1){page--;load()}};$('next').onclick=()=>{if(page<Math.ceil(total/limit)){page++;load()}};$('export').onclick=()=>{location.href='/api/checkins/export?'+params({page:1,limit:10000})};$('close').onclick=()=>$('detail').close();load();